---
title: N-007: Dialogue feedback loop
---

# Description

The dialogue starts when the user sends the first message. Then a feedback loop is initiateed where the AI plans an action, tool call is output, the app parses that and calls the tool, returns the tool output to the AI, which then plans the next action until the final answer is reached.
